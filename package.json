{
  "name": "dam-bot",
  "version": "0.0.0",
  "private": true,
  "description": "Claude Code agent running as dam-bot. Houses skills the bot performs on a schedule.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
